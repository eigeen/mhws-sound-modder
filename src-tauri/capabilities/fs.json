{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs",
  "description": "",
  "windows": ["main"],
  "permissions": [
    "fs:default",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "**/*" }]
    },
    "fs:allow-exists",
    "fs:allow-read-dir"
  ]
}
